# ===========================================
# Flask 應用程式基本設定
# ===========================================
FLASK_APP="flask_weather"
FLASK_ENV="production"
SECRET_KEY="your-secret-key-here"
OPENWEATHER_API_KEY="your-openweather-api-key-here"
CWA_API_KEY="your-cwa-api-key-here"

# HTTPS 強制執行 (Zeabur 上建議設為 True)
TALISMAN_ENABLED="True"

# ===========================================
# Zeabur PostgreSQL 環境變數
# ===========================================
# 在 Zeabur 上部署時，以下變數會自動注入，無需手動設定
# POSTGRES_CONNECTION_STRING=postgresql://user:password@host:port/database
# POSTGRES_HOST=postgres.zeabur.internal
# POSTGRES_PORT=5432
# POSTGRES_USERNAME=root
# POSTGRES_PASSWORD=auto-generated-password
# POSTGRES_DATABASE=postgres

# 本地開發時可使用：
# DATABASE_URL=postgresql://localhost:5432/flask_weather

# ===========================================
# Zeabur Redis 環境變數
# ===========================================
# 在 Zeabur 上部署時，以下變數會自動注入，無需手動設定
# REDIS_CONNECTION_STRING=redis://:password@host:port
# REDIS_HOST=redis.zeabur.internal
# REDIS_PORT=6379
# REDIS_PASSWORD=auto-generated-password

# 本地開發時可使用：
# RATELIMIT_STORAGE_URI=redis://localhost:6379